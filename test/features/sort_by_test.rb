require "test_helper"

feature "SortBy" do
  scenario "the test is sound" do
    skip " this aasdfkjadkls;f"
    visit root_path
    #click ok drop down
      #check to make sure the page load right
    #cycle through options    
  end
end
